structs are classes with all public functions and variables

structs don't take up any space, all of its operations and identities are resolved in the compiler

use -> to access a member from a pointer struct

use the following so you can refer to structs as their name instead of saying struct everytime:
typedef (struct s) s;
