T* a - make a pointer variable of type T
&x - return the address of the variable, not the value

&a - get the address of the pointer, not the address in its value

$*p$ - dereference the pointer and retrieve the value of the pointer's stored address