#ComputerArchitecture
$$Computer = Data Path + Controller$$

Data flows through data path, controller controls which direction the data flows through, similar to a conductor switching the right train tracks for the trains to go.

Every train track is always there, so when we design the data path, we make sure everything is there and available, and which one to pick depends on the controller.