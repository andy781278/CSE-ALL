---
aliases:
  - family of distributions
  - distribution form
---
$$X \textasciitilde Name(\theta)$$
X is distributed according to the Name distribution with parameter $\theta$

We have some $X_1,X_2,...,X_n \textasciitilde Name(\theta)$ with n iid samples from some unknown distribution

We want to understand $\theta$, since it is some kind of unknown population parameter, we can estimate it via
$$\hat{\theta} \equiv \hat{\theta}(X_1,X_2,...,X_n)$$
from the samples we collect, $\hat{\theta}$ is the statistic