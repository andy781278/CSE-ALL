In regression, we fit a line to a bunch of points

The loss function of a typical regression goes by the form:
$$L(w)=\sum_{i=1}^nl(w;x^{(i)}y^{(i)})$$
where $l(w;x^{(i)}y^{(i)})$ captures the loss on a single point

