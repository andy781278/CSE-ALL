A non-specific component with inputs A and B, and output is determined via selector bit that support up to $2^n$ different operations on n selector bits.

Using a demux, A and B are connected to various gates that operates on them, them connects them to the corresponding demux sequence and is selected according to the selector bits.

